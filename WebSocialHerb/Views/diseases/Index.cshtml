@model IEnumerable<WebSocialHerb.Models.disease>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DiseaseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Syntom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Herb)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DiseaseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Syntom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Herb)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DiseaseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DiseaseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DiseaseID })
        </td>
    </tr>
}

</table>
